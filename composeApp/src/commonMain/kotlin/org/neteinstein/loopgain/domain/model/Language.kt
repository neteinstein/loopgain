package org.neteinstein.loopgain.domain.model

/** The app's display language. The deck, the session flow copy and the Settings screen are fully authored in all four. */
enum class Language(val nativeName: String) {
    EN("English"),
    PT("Português"),
    ES("Español"),
    FR("Français"),
}
